const {mitosi} = require('../');
const chai = require('chai');

chai.use(require('chai-things'));
chai.should();

describe('Mitosi', () => {
    before(done => {
    });

    describe('Test', () => {
    });

    after(() => {
    });
});